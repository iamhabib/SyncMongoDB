services:
  # Local MongoDB — the sync target. Change Streams write here.
  mongo:
    image: ${MONGO_IMAGE:-mongo:7}
    container_name: mongo-sync-target
    restart: unless-stopped
    ports:
      - "127.0.0.1:${LOCAL_MONGO_PORT:-27017}:27017"
    volumes:
      - ./DATA_SOURCE:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${LOCAL_MONGO_ROOT_USER:?Set LOCAL_MONGO_ROOT_USER in .env}
      MONGO_INITDB_ROOT_PASSWORD: ${LOCAL_MONGO_ROOT_PASSWORD:?Set LOCAL_MONGO_ROOT_PASSWORD in .env}
    healthcheck:
      test: [ "CMD", "mongosh", "--quiet", "-u", "${LOCAL_MONGO_ROOT_USER}", "-p", "${LOCAL_MONGO_ROOT_PASSWORD}", "--eval", "db.adminCommand('ping')" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Change Stream sync service: remote MongoDB -> local Mongo
  sync:
    build:
      context: ./mongodb-sync
    container_name: mongodb-oplog-sync
    restart: unless-stopped
    depends_on:
      mongo:
        condition: service_healthy
    env_file:
      - .env
    environment:
      LOCAL_MONGO_URL: mongodb://${LOCAL_MONGO_ROOT_USER}:${LOCAL_MONGO_ROOT_PASSWORD}@mongo:27017/${MONGO_DATABASE_NAME:-sync_db}?authSource=admin
      HEALTH_BIND_HOST: 0.0.0.0
    ports:
      # Bound to localhost only — do not expose health/metrics publicly
      - "127.0.0.1:${PORT:-3000}:${PORT:-3000}"
    volumes:
      - ./LOGS:/app/logs
    healthcheck:
      test: [ "CMD", "node", "-e", "require('http').get('http://127.0.0.1:' + (process.env.PORT || 3000) + '/health', r => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))" ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1024M
        reservations:
          memory: 512M
